import { ItemPedido } from "@/lib/models/ItemPedido";
import { Pedido } from "@/lib/models/Pedido";


export class PedidoService {
    async create({ mesa, itens }: { mesa: number, itens: ItemPedido[] }) {

        // const pedido = new Pedido(,)
    }
}